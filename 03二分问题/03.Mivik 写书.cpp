#include<iostream>
#include <vector>
#include <cstdio>
#include <string>
#include <algorithm>
#include <math.h>
#include <iomanip> 
#include <map>
using namespace std;

int main() {
	int n, m;
	char* array = new char[10];
	cin >> n >> m;
	char ch;
	for (int i = 1; i <= m; i++) {
		cin >> ch;
		array[i] = ch;
	}
	map<int, int>a;
	for (int i = 0; i < )
		return 0;


}